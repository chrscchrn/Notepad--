<div class="notes fill">
    <a href="/work?num={{num}}">
        <article class="note-textarea" placeholder="">
            <h5 class="note-title">
                <strong>{{this.dataValues.title}} - Click to Edit <i class="material-icons" id="icons">edit</i></strong>
            </h5>
            
            <strong>{{this.dataValues.noteTitle}}</strong>
            </br>
            <a id="savedURL" href="{{this.dataValues.url}}">Link to Article Wesbite <i class="material-icons" id="icons">link</i></a>
        </article>
    </a>
</div>